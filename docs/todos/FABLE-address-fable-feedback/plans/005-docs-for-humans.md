# Docs for Humans

**Plan #:** 005
**Date:** 2026-08-11
**Related Todo:** [../todo.md](../todo.md)
**Status:** Draft
**Last Updated:** 2026-08-11
**Plan-review opt-in:** (declared at draft time)
**Code-review opt-in:** (declared at draft time)

---

## Scope

Close the human-docs gap FableFeedback.md identifies: write the missing guides (lazy loading, authorization, testing, trimming — ~48 orphaned compiled sample regions in `src/samples` already exist to power them), port the skill-only doctrine (three-phase pattern, aggregate-as-graph, `Parent`) into the docs set, replace the alphabetical guide ordering with a pedagogical track, link the vsCSLA material for evaluators, refresh stale `UPDATED` stamps against the current API (`MarkReadOnly`, `ILogger`), and fix the Person example README (SQLite not SQL Server, actual project list, how-to-run instructions). Use the `/docs-create`/`/docs-update` workflow per project convention. This plan does NOT rewrite the skills themselves; skill/API drift is tracked separately if it grows beyond incidental fixes.
