#nullable enable
using Neatoo;
using Microsoft.Extensions.DependencyInjection;
using System.ComponentModel;
using System.ComponentModel.DataAnnotations;
using Neatoo.Internal;
using Neatoo.RemoteFactory;
using Neatoo.Rules;
using Xunit;

/*
DO NOT MODIFY
Generated by Neatoo.BaseGenerator
*/
namespace Samples
{
    public partial class BlazorEmployee
    {
        protected IValidateProperty<string> NameProperty => (IValidateProperty<string>)PropertyManager[nameof(Name)]!;
        protected IValidateProperty<string> EmailProperty => (IValidateProperty<string>)PropertyManager[nameof(Email)]!;
        protected IValidateProperty<decimal> SalaryProperty => (IValidateProperty<decimal>)PropertyManager[nameof(Salary)]!;
        protected IValidateProperty<DateTime?> StartDateProperty => (IValidateProperty<DateTime?>)PropertyManager[nameof(StartDate)]!;
        protected IValidateProperty<bool> IsActiveProperty => (IValidateProperty<bool>)PropertyManager[nameof(IsActive)]!;
        protected IValidateProperty<Priority> PriorityProperty => (IValidateProperty<Priority>)PropertyManager[nameof(Priority)]!;
        protected IValidateProperty<string> DepartmentProperty => (IValidateProperty<string>)PropertyManager[nameof(Department)]!;
        protected IValidateProperty<string> NotesProperty => (IValidateProperty<string>)PropertyManager[nameof(Notes)]!;

        public partial string Name
        {
            get => NameProperty.Value;
            set
            {
                NameProperty.Value = value;
                if (!NameProperty.Task.IsCompleted)
                {
                    Parent?.AddChildTask(NameProperty.Task);
                    RunningTasks.AddTask(NameProperty.Task);
                }
            }
        }

        public partial string Email
        {
            get => EmailProperty.Value;
            set
            {
                EmailProperty.Value = value;
                if (!EmailProperty.Task.IsCompleted)
                {
                    Parent?.AddChildTask(EmailProperty.Task);
                    RunningTasks.AddTask(EmailProperty.Task);
                }
            }
        }

        public partial decimal Salary
        {
            get => SalaryProperty.Value;
            set
            {
                SalaryProperty.Value = value;
                if (!SalaryProperty.Task.IsCompleted)
                {
                    Parent?.AddChildTask(SalaryProperty.Task);
                    RunningTasks.AddTask(SalaryProperty.Task);
                }
            }
        }

        public partial DateTime? StartDate
        {
            get => StartDateProperty.Value;
            set
            {
                StartDateProperty.Value = value;
                if (!StartDateProperty.Task.IsCompleted)
                {
                    Parent?.AddChildTask(StartDateProperty.Task);
                    RunningTasks.AddTask(StartDateProperty.Task);
                }
            }
        }

        public partial bool IsActive
        {
            get => IsActiveProperty.Value;
            set
            {
                IsActiveProperty.Value = value;
                if (!IsActiveProperty.Task.IsCompleted)
                {
                    Parent?.AddChildTask(IsActiveProperty.Task);
                    RunningTasks.AddTask(IsActiveProperty.Task);
                }
            }
        }

        public partial Priority Priority
        {
            get => PriorityProperty.Value;
            set
            {
                PriorityProperty.Value = value;
                if (!PriorityProperty.Task.IsCompleted)
                {
                    Parent?.AddChildTask(PriorityProperty.Task);
                    RunningTasks.AddTask(PriorityProperty.Task);
                }
            }
        }

        public partial string Department
        {
            get => DepartmentProperty.Value;
            set
            {
                DepartmentProperty.Value = value;
                if (!DepartmentProperty.Task.IsCompleted)
                {
                    Parent?.AddChildTask(DepartmentProperty.Task);
                    RunningTasks.AddTask(DepartmentProperty.Task);
                }
            }
        }

        public partial string Notes
        {
            get => NotesProperty.Value;
            set
            {
                NotesProperty.Value = value;
                if (!NotesProperty.Task.IsCompleted)
                {
                    Parent?.AddChildTask(NotesProperty.Task);
                    RunningTasks.AddTask(NotesProperty.Task);
                }
            }
        }

        /// <summary>
        /// Generated override for stable rule identification.
        /// Maps source expressions to deterministic ordinal IDs.
        /// </summary>
        protected override uint GetRuleId(string sourceExpression)
        {
            return sourceExpression switch
            {
                @"async e => { if (string.IsNullOrEmpty(e.Email)) return """"; // Simulate async validation (e.g., checking against external service) await Task.Delay(50); if (!e.Email.EndsWith(""@company.com"", StringComparison.OrdinalIgnoreCase)) { return ""Email must be a company email (@company.com)""; } return """"; }" => 1u,
                @"EmailAddressAttribute_Email" => 2u,
                @"RangeAttribute_Salary" => 3u,
                @"RequiredAttribute_Name" => 4u,
                _ => base.GetRuleId(sourceExpression) // Fall back to hash for unknown expressions
            };
        }

        /// <summary>
        /// Generated override to initialize property backing fields.
        /// </summary>
        protected override void InitializePropertyBackingFields(IPropertyFactory<Samples.BlazorEmployee> factory)
        {
            // Initialize and register this class's properties
            // The backing field properties are computed and fetch from PropertyManager
            PropertyManager.Register(factory.Create<string>(this, nameof(Name)));
            PropertyManager.Register(factory.Create<string>(this, nameof(Email)));
            PropertyManager.Register(factory.Create<decimal>(this, nameof(Salary)));
            PropertyManager.Register(factory.Create<DateTime?>(this, nameof(StartDate)));
            PropertyManager.Register(factory.Create<bool>(this, nameof(IsActive)));
            PropertyManager.Register(factory.Create<Priority>(this, nameof(Priority)));
            PropertyManager.Register(factory.Create<string>(this, nameof(Department)));
            PropertyManager.Register(factory.Create<string>(this, nameof(Notes)));
        }
    }
}